import { connection } from "./connection";


	const userTableName = "auth_User";

	export const createUserData = async (
		id: string, 
		email: string, 
		password: string) => {
	  await connection
	    .insert({
	      id,
	      email,
	      password,
	    })
	    .into(userTableName);
	};